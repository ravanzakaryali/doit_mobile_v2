import { StyleSheet } from 'react-native';
import { fontSizes, colors } from 'src/themes';


export default StyleSheet.create({
    textStyle: {
        fontSize: fontSizes.small,
        color: colors.black,
    }
});
